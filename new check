# Тест 6 — fullscan з обома параметрами в URL
res = requests.post(
    f"{BASE_URL}/command-queue/fullscan?group_ids={group_id}&computer_ids={comp_id}",
    headers=headers,
    verify=False
)
print(f"Тест 6: {res.status_code} | {res.text[:200]}")

# Тест 7 — activescan з group_ids в URL
res = requests.post(
    f"{BASE_URL}/command-queue/activescan?group_ids={group_id}&computer_ids={comp_id}",
    headers=headers,
    verify=False
)
print(f"Тест 7: {res.status_code} | {res.text[:200]}")

# Тест 8 — fullscan з hardware_ids в URL
res = requests.post(
    f"{BASE_URL}/command-queue/fullscan?hardware_ids={comp_id}",
    headers=headers,
    verify=False
)
print(f"Тест 8: {res.status_code} | {res.text[:200]}")
